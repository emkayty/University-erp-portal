# Generated performance fixtures

These JSON files are generated by `pnpm --filter @uniportal/api run seed:k6-test-students -- --count 5000 --env staging` and are intentionally not committed with credentials or tokens.

Do not run the seeder against production. Use a dedicated staging/test database and short-lived JWTs.
